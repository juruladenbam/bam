# Admin Panel Environment Configuration
# 
# Note: Admin panel uses relative paths (/api/admin) for API calls,
# so no API URL configuration is needed. The app will automatically
# use the same domain as the frontend.
#
# Additional environment variables can be added here as needed.
